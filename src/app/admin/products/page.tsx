// This file is a duplicate created in error and should not be treated as a page.
// A file named `page.tsx` is only considered a page by Next.js if it has a default export.
// By removing the default export, we prevent it from being included in the routing system,
// which resolves the "parallel pages" build error.
export const conflictResolution = true;
